version: "3.8"

services:
  dossansdouleur_wp:
    container_name: dossansdouleur_wp
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    networks:
      - global_proxy_network

networks:
  global_proxy_network:
    external: true